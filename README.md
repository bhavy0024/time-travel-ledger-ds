# time-travel-ledger-ds
Built a version-controlled financial ledger in C++ using persistent data structures. Each transaction creates a new version, allowing queries on past states, comparison between versions, and basic fraud detection. Demonstrates efficient memory sharing and O(log n) operations.
